export interface RaceType {
    EventID: string;
    RaceNum: string;
    Description: string;
    RaceType: string;
    SuspendDateTime: string;
    SuspendTime: number;
}

export interface CompetitorType {
    Name: string;
    PositionNumber: number;
}